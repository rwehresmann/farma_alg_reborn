showModal('connection', true, function(modalName) {
  var div = $(document.createElement('div'))
  div.attr('id', modalName + '-container');
  $('body').append(div);
  $(div).html("<%= escape_javascript(render 'connection_modal') %>");
  $('body').append(div);
});
