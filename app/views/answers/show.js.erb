function createModal(modal_name) {
  var div = $(document.createElement('div'))
  div.attr('id', modal_name + '-container');
  $('body').append(div);
  $(div).html("<%= escape_javascript(render 'answer_modal') %>");
  $('body').append(div);
}

showModal('answer', true);
