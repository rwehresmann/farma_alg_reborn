showModal('answer', true, function(modalName) {
  var div = $(document.createElement('div'))
  div.attr('id', modalName + '-container');
  $('body').append(div);
  $(div).html("<%= escape_javascript(render 'answer_modal') %>");
  $('body').append(div);
});
