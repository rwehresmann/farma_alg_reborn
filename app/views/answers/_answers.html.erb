<table id="answers-table" class="table table-bordered table-hover dataTable" role="grid">
  <thead>
    <tr role="row">
      <th class="sorting" style="width: 150px" aria-sort="ascending">Turma</th>
      <th class="sorting" aria-sort="ascending">Exercício</th>
      <th class="sorting">Questão</th>
      <th class="sorting" style="width: 10px">Tentativa</th>
      <th class="sorting" style="width: 10px">Resposta</th>
  </thead>

  <tbody>
    <% @answers.each do |answer| %>
      <tr role="row" class="odd" data-link="<%= new_answer_question_path(answer.question, answer.team, answer_id: answer.id) %>">
        <td>
          <%= answer.team.name %>
        </td>
        <td>
          <%= answer.question.exercise.title %>
        </td>
        <td>
          <%= answer.question.title %>
        </td>
        <td>
          <%= answer.attempt %>
        </td>
        <td>
          <%= answer_status_label(answer) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
