$('#exercises-table').html(
  '<%= j( \
    render( \
      partial: "teams/exercises/teacher_exercises_table", \
      locals: { \
        team: @team, \
        team_exercises: @team_exercises \
      } \
    ) \
  )%>'
);

$('#add-exercise-<%= @exercise.id %>').html(
  '<%= j( \
    render( \
      partial: "teams/exercises/add_exercise", \
      locals: { \
        exercise: @exercise, \
        team: @team \
      } \
    ) \
  ) %>'
);
